#include<stdio.h>
#include<stdlib.h>

void Signup()
{
    system("clear");
    sign_up(arr, &pnum);
}
void Signin()
{
    system("clear");
    sign_in(arr, &pnum, &osnum);
}

void Listofuser()
{
    system("clear");
    list_of_user(arr, &pnum);
}
